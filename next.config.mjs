/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export',       // build 时导出成静态资源
  // distDir: 'dist',      // 用于定义构建输出文件夹的名称，默认是 .next
  // assetPrefix: '/note', // 配置静态资源访问路径
  // basePath: '/note', // 配置路由跳转时的前baseUrl
  //   exportPathMap: async function () {
  //     return {
  //       "/": { page: "/note" },
  //     };
  //   },
};

export default nextConfig;
